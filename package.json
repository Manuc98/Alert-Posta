{
  "name": "alertapostas-worker",
  "version": "1.0.0",
  "description": "Alert@Postas Cloudflare Worker - Proxy e cache para API principal",
  "main": "src/index.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "deploy:production": "wrangler deploy --env production",
    "build": "echo 'No build step required for this worker'",
    "test": "echo 'No tests configured'",
    "lint": "eslint src/",
    "format": "prettier --write src/"
  },
  "keywords": [
    "cloudflare-worker",
    "alertapostas",
    "proxy",
    "cache",
    "api"
  ],
  "author": "Alert@Postas Team",
  "license": "MIT",
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240911.0",
    "eslint": "^8.57.0",
    "prettier": "^3.2.5",
    "wrangler": "^3.78.12"
  },
  "dependencies": {},
  "engines": {
    "node": ">=18.0.0"
  }
}
